name: C CI

on:
  push:
    branches: [ main ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - name: make
      run: make
    - name: make tests
      run: make test1 test2
    - name: make check
      run: make check
